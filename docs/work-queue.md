# Work queue

The single source of "what's next". Process rules: [process.md](process.md). Dependencies & rationale: [execution plan](tasks/00-execution-plan.md).

## Protocol (per item)
1. Take the **first `todo` item whose dependencies are `done`** (items with **different** lane letters can run in parallel in separate agent sessions; items within one lane run in lane order).
2. Set `in-progress`, load **only** the task file + its *Read first* list.
3. Implement with tests ([testing strategy](testing-strategy.md)); **local verification green**: `dotnet build DocHub.slnx -c Release` + `dotnet test --solution DocHub.slnx -c Release` on a clean copy of the tree (CI is deferred — decisions log Q17).
4. Set `in-review`; request review with the template in [process §5](process.md#5-review-request-template-keep-it-this-small) in a fresh context.
5. Fix valid findings → re-review delta → repeat until **GREEN**.
6. Set `done`, write a handoff note (≤ 5 lines) in the table below, including actuals: wall-clock, review rounds (process §9).

Status: `todo` · `in-progress` · `in-review` · `done` · `blocked` · `dropped`

Q22 is placed right after Q04 (it hardens the Q03 schema before feature work builds on it).

## Queue

| # | Item | Artifact | Depends on | Lane | Status | Review |
|---|---|---|---|---|---|---|
| Q00 | Review requirements & task specs (this doc set) | REQ + SPEC | — | – | done | **GREEN** after 7 rounds |
| Q01 | [T01](tasks/T01-api-solution-setup.md) API solution | CODE | Q00 | A | done | **GREEN** (2 rounds) |
| Q02 | [T02](tasks/T02-database-project-and-schema.md) DB project & schema | DB + TEST | Q01 | A | done | **GREEN** (2 rounds) |
| Q03 | [T03](tasks/T03-database-change-tracking.md) Audit triggers | DB + TEST | Q02 | A | done | **GREEN** (3 rounds) |
| Q04 | [T04](tasks/T04-api-foundation.md) API foundation | CODE + TEST | Q01, Q03 | A | done | **GREEN** (3 rounds) |
| Q22 | [T21](tasks/T21-tamper-evident-audit.md) Tamper-evident audit: ledger, deploy-script guard, reconciliation | DB + CODE + TEST | Q04 | A | done | spec GREEN (6 rounds); code review deferred by the product owner |
| Q05 | [T05](tasks/T05-users-and-node-types-api.md) Users, node types, content styles | CODE + TEST | Q04 | B | done | **GREEN** (2 rounds) |
| Q06 | [T06](tasks/T06-virtual-folders-api.md) Folders | CODE + TEST | Q04 | B | done | **GREEN** (2 rounds) |
| Q07 | [T07](tasks/T07-documents-and-versions-api.md) Documents, signing, restore | CODE + TEST | Q04, Q06 | A | done | **GREEN** (3 rounds) |
| Q08 | [T14](tasks/T14-web-shell-folders-documents.md) Web shell & lists | CODE + TEST | Q05, Q06, Q07 | C | done | **GREEN** (3 rounds) |
| Q09 | [T10](tasks/T10-permissions.md) Permissions | CODE + TEST | Q07 | B | done | **GREEN** (2 rounds) |
| Q10 | [T08](tasks/T08-document-tree-api.md) Tree | CODE + TEST | Q05, Q07 | A | done | **GREEN** (3 rounds) |
| Q11 | [T09](tasks/T09-node-content-api.md) Styled content | CODE + TEST | Q05, Q10 | A | done | **GREEN** (3 rounds) |
| Q12 | [T17](tasks/T17-web-admin-tab.md) Web admin | CODE + TEST | Q05, Q08, Q14, Q22 | C | todo | – |
| Q13 | [T13](tasks/T13-comments-api.md) Comments | CODE + TEST | Q07, Q09 | B | done | **GREEN** (1 round) |
| Q14 | [T11](tasks/T11-change-history-api.md) History + diff engine | CODE + TEST | Q03, Q11, Q22 | A | done | **GREEN** (9 rounds; R9-1 accepted — decisions log Q20) |
| Q15 | [T12](tasks/T12-version-comparison-api.md) Compare | CODE + TEST | Q11, Q14 (diff engine) | B | done | **GREEN** (2 rounds) |
| Q16 | [T15](tasks/T15-web-document-editor.md) Web section editor + inline history | CODE + TEST | Q08, Q09, Q10, Q11, Q14 | C | in-review | round 1 |
| Q17 | [T16](tasks/T16-web-compare-comments-permissions.md) Web compare/comments/permissions | CODE + TEST | Q09, Q13, Q15, Q16 | C | todo | – |
| Q18 | ~~T18 Search~~ — out of scope (decisions log Q13) | – | – | – | dropped | – |
| Q21 | [T20](tasks/T20-pdf-export.md) PDF export (API, worker, UI) | CODE + TEST | Q05, Q07, Q09, Q11, Q16 | B | todo | – |
| Q19 | [T19](tasks/T19-load-and-performance.md) Load & performance harness + tuning | TEST + CODE | Q11 (harness), Q13, Q14, Q15, Q16, Q21 (full mix) | B | todo | – |
| Q20 | Release-candidate E2E & NFR pass (full Playwright, full-scale load + soak, a11y, mutation report, DACPAC drift report) | TEST | Q12, Q17, Q19 | – | todo | – |

Lanes: **A** backend core / DB · **B** backend features · **C** frontend.

## Handoff notes
- **Q00** — Doc set reviewed in 7 fresh-context rounds (process §4, REQ/SPEC rule): 19 → 12 → 11 → 7 → 7 → 1 → 0 valid findings, all fixed. Product input added during review (data access via EF + SPs, load profile, in-editor history, PDF export) was reviewed in the same loop. Next: Q01.
- **Q01** — .NET 10 solution (`DocHub.slnx`), CPM, warnings-as-errors, `/health`, OpenAPI+Scalar (dev only), xUnit v3 on Microsoft.Testing.Platform (`dotnet test --solution`), docker-compose SQL 2022, CI on every push/PR (green). Deviation: SDK installed from Ubuntu apt (dot.net blocked by proxy); CI now runs on all branch pushes. Review: 2 findings fixed → GREEN. Actuals: ≈ 40 min wall-clock (incl. SDK install and one CI run), 2 review rounds.
- **Q02** — `database/DocHub.Database` (Microsoft.Build.Sql, Azure SQL): 13 tables, constraints, load-profile indexes, roles/grants, idempotent seed that never overwrites admin data. `DocHub.Testing`: Testcontainers SQL fixture + DacFx deploy (DACPAC built via the solution). 31 tests incl. redeploy drift check; CI job publishes with SqlPackage + deploy-report drift gate (green). Follow-ups: optional demo-document seed deferred; tool manifest at repo root. Actuals: ≈ 50 min, 2 review rounds (fixed: Release DACPAC path, seed re-linking styles).
- **Q03** — `audit.ChangeLog` (append-only, page-compressed, static monthly partitions 2026–2035) + 11 generated set-based triggers (`dotnet run database/tools/GenerateAuditTriggers.cs`). `Source='App'` requires the `app_api`/`db_owner` role — scripts can't pose as the API. Derived-only NodeContent updates unaudited only for the API; script edits → audited, `DerivedStale`, tampering. `VersionStamp` trigger-only (DENY); tampering covers moves, (un/re)signing by script, signature changes. For T04+: API must connect as an `app_api` member; EF needs `HasTrigger`; API content save sets `DerivedStale = 0`; signing inserts the last signature before finalizing. Actuals: ≈ 2 h, 3 review rounds (7 findings, all support-script bypasses). CI deferred (Q17).
- **Q04** — EF Core 10 mapping of all app tables (`HasTrigger`, no migrations, drift test incl. unmapped/hidden columns), session-context interceptor, `IDbProcedures` + `usp_Ping`, test-mode auth (`X-User-Id`, prod guard), `ICurrentUser`, `/api/me`, `/api/system/info`, ProblemDetails from one place (`ProblemDefaults`, JSON even for non-JSON `Accept`), validation filter, paging, CORS, DB health, Minimal API `IEndpointModule`s, OpenAPI snapshot `src/DocHub.Api/openapi.v1.json`. Tests: DB per class, API as an `app_api` login, authz-matrix harness; 44 API tests. **Follow-up for Q05:** map `BadHttpRequestException` 413/415 to `payload-too-large`/`unsupported-media-type` (currently `validation-failed`; unreachable until body endpoints exist). Actuals: ≈ 2 h, 3 review rounds.
- **Q22** — Audited tables temporal + updatable ledger, `ChangeLog`/findings/baseline append-only ledger; generated `audit.usp_ReconcileLedger` (rules 1–5, allow-list), `vModifiedAfterSigning` (use it for T07 `modifiedAfterSigning`), module-hash check + nightly `LedgerReconciliationService` + `POST /api/admin/audit/reconcile`, `GET /api/admin/audit/findings`; deploy-script guard; runbook `docs/runbooks/tamper-evidence-azure.md`. Existing DBs must be recreated. Local API must connect as an `app_api` login (else forged-row findings). Code review **deferred** by the product owner ("skip tampering tests for now, build core features"); do a review pass before go-live. Actuals: ≈ 5 h, 0 review rounds. Weak-environment profile: decisions log Q19 (2 actors, 10 s, 2 test workers).
- **Q05** — `/api/users` (active, paged ≤ 100, prefix search; indexes on display name/e-mail), node types and content styles CRUD (admin writes, `rowVersion`, usage counts, `409 in-use` / `built-in-style` / `duplicate-name`), style property schema + generated `stylesheet.css` with ETag (`StyleProperties` in Infrastructure — reuse for the T09 validator/renderer), 413/415 mapping done. Tagged perf tests (50 500 users, node/style grids) use median of 5 after warm-up. Actuals: ≈ 1.5 h, 2 review rounds (4 findings fixed: case-insensitive base styles, robust CSS, paging overflow, `\z` colors).
- **Q06** — Folder tree/details/CRUD/move (admin writes; app lock + bounded cycle walk; `TR_Folder_NoCycle` rejects cycles from scripts too), gap-based `Domain.Ordering.SiblingOrder` (reuse for T08 nodes), delete `409 in-use` incl. deleted documents, FK races → `in-use`/`not-found`. Actuals: ≈ 1 h, 2 review rounds (2 findings fixed). Note: if READ_COMMITTED_SNAPSHOT is ever enabled, revisit the cycle trigger (concurrent script + move).
- **Q07** — Documents CRUD/delete/restore, drafts, discard, all-approver signing with the canonical tree hash (`VersionTreeHash`, `CanonicalJson` raw-hash fallback), SP-backed list with cached hashes (`VersionContentHash`), `IDocumentAuthorization`/`IVersionGuard`. The per-document app lock `document:{id}` serializes every lifecycle and structural change; deletion and editability are re-checked under it. `TR_DocumentNode_Tree` rejects cycles and more than 100 levels. Actuals: ≈ 4 h, 3 review rounds (6 findings fixed).
- **Q10** — Tree read (ETag = version stamp, signed trees in HybridCache with a depth-256 serializer), node get/create/patch/move/delete (`usp_DeleteSubtree`), all via `NodeRules.MutateAsync` (lock + editability re-check — reuse for T09/T10 writes). Actuals: ≈ 2 h, 3 review rounds (3 findings fixed).
- **Q11** — Content Schema v1 validator/canonicalizer (`ContentDocument`) and renderer (`ContentHtmlRenderer`) in Domain; GET/PUT content, batch read, `/api/content-schema`; PUT under the document lock; `DerivedContentRefresher` (30 s, system user). Content ETags = stamp + row versions (the refresher changes rows without a stamp). Script-stored lone surrogates are repaired before reading (`ContentSchema.RepairLoneSurrogates`); the hash stays raw. Fixtures + HTML snapshots in `tests/DocHub.Domain.Tests/ContentFixtures`. Actuals: ≈ 3 h, 3 review rounds (5 findings fixed).
- **Q09** — `usp_GetEffectivePermissions`, grants CRUD + `my-permissions` (same record as `myRoles`), per-request cached `IDocumentAuthorization` with `RecheckAsync` under the lock (sign, content save), revoke withdraws the approver's draft signature and re-runs finalization. `EditContent` requires a version. Transfer of ownership not built (optional). Comment/resolve rights are checked by flags until T13. Actuals: ≈ 2 h, 2 review rounds (4 findings fixed).
- **Q13** — Comments on documents/nodes of Draft and Signed versions, one reply level, author edit, author/owner soft delete, owner/approver resolve, counts, `includePreviousVersions`. Open product question: a user whose role was revoked may still edit/delete their own comments (spec: "author only"). Actuals: ≈ 45 min, 1 review round.
- **Q15** — Compare (base=id|latestSigned, target=id|draft): summary, merged tree (removed nodes after their nearest preceding base sibling), LCS reorder, content stats, per-node diff. Signed pairs cached by stamps (type codes resolved per read). Content loaded in chunks of 500 ids (parameter limit). Actuals: ≈ 1.5 h, 2 review rounds (4 findings fixed).
- **Q08** — `/web`: Vite + React 19 + TS strict + Mantine 9, TanStack Query, generated `openapi-fetch` client (`X-User-Id`, ProblemDetails → `ApiError`, toasts, `describeError`). Shell with test-mode chip and "Acting as" (type-ahead, recent users, Ctrl+Shift+U; a user is confirmed by `/api/me` before switching; a stale stored user falls back to `defaultUserId`). Folder tree (admin actions, row version taken when the dialog opens, deep links), document list cards (server sort, show deleted, paging, create/delete/restore/move). Visual language per the product owner's reference images: `web/src/styles.css` + `web/src/theme.ts` (`variant="soft"`). Tests: Vitest against `src/test/fakeApi.ts`, Playwright smoke (`npm run e2e`, needs the running stack). CI web job added (manual). For T15: acting-as quick picks for the open document's owner/editors/approvers. Actuals: ≈ 4 h, 3 review rounds (6 findings fixed; 2 more found by tests).
- **Q14** — Node/document history (grouped by correlation id; moves with parent titles), entry diff/content, `change-summary`, `/changes` track changes since a baseline (latest signed / `v:` / `e:` / `d:`), `/api/admin/audit`. Diff engine `IContentDiffService` (DiffPlex, block alignment, Jaccard pairing, format ops) and `AttributedDiff` (character fold: inserted/format/moved/displaced/placed marks, linear `MarkMoves` per baseline run). Perf: 1 M-row history test, 2000-paragraph reorders. Known limitation: fragment moves between paragraphs (decisions log Q20). Actuals: ≈ 6 h, 9 review rounds (≈ 20 findings fixed, mostly attribution of moves).
